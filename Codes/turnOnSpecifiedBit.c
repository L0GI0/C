#include <stdio.h>
#include <stdlib.h>

int main(int argc, char const *argv[])
{
	if(!isdigit(argv[1]) || !isdigit(argv[1]) || argc =! 3){
		fprint(stderr, "Invalid arguments input");
		exit(EXIT_FAILURE);
		int val = 
	}
	return 0;
}